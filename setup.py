#!/usr/bin/env python
# -*- coding: utf-8 -*-

from setuptools import setup, find_packages


setup(name='ComssService',
      version='0.1',
      install_requires=[
      ],
      packages=find_packages(),
      include_package_data=True,
      )
